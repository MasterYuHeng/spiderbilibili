# Optional frontend overrides for local development.
# By default Vite will proxy /api requests to http://127.0.0.1:8014.
# Only set this when you want the frontend to talk to a remote backend.

VITE_API_BASE_URL=http://127.0.0.1:8014
